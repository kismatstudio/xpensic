node_modules/
*.db
*.db-journal
*.db-shm
*.db-wal
*.log
*.pid
*.sqlite
*.sqlite3
expense-tracker.test.db
.wrangler/

# CSV storage (auto-created at runtime; never commit user data)
data/
*.csv
data/*.csv

# Migrated legacy JSON (one-shot; safe to ignore)
expense-tracker.db.json
expense-tracker.db.json.migrated

# Local secrets — never commit. Copy .env.example to .env and fill in.
.env
.env.local
.env.*.local
